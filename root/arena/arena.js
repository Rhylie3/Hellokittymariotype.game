class Arene {
  constructor(x, y, width, height, tpye, timer) {
    this.x = x;
    this.y = y;
    this.width = width;
    this.height = height;
    this.type = type;
  }

  draw() {
    if (thid.type === 1) {
    } else if (this.type === 2) {

    } else if (this.type === 3) {

    } else if (this.type === 4) {

    } else if (this.type === 5) {

    }
  }
}

hello
